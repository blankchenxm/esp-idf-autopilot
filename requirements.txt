# serial_mcp.py - standalone serial-monitor MCP server (Windows).
# No ESP-IDF dependency; only the MCP SDK and pyserial.
mcp>=1.2.0
pyserial>=3.5
pydantic>=2.10,<3
jsonschema>=4.23,<5
langgraph==1.2.9
langgraph-checkpoint-sqlite==3.1.0
pytest>=8,<9
